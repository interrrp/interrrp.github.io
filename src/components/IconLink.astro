---
import { Icon } from "astro-icon/components";

const { name, label, href } = Astro.props;
---

<a href={href} aria-label={label} class="hover:opacity-80 duration-100"
  ><Icon name={name} size={24} /></a
>
